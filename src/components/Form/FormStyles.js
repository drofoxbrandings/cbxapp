import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    errorMsg: {
        fontSize: '14px',
        color: '#f44336'
    },
    formField: {
        margin: '1rem 0',
    },
    selfEnd: {
        display: 'flex',
        justifyContent: 'flex-end'
    }
})); 